# Codex 开发任务：A股每日14:45量化监测与筛选系统

## 项目目标
从零开发一个可在 Windows 本地长期运行的 A 股量化监测与筛选程序。仅用于监测和筛选，不连接券商、不自动下单。

支持：交易日14:45实时筛选、收盘后复盘、指定历史交易日回测；保存每日筛选结果、指标和淘汰原因，用于长期统计优化。

## 股票池
仅上海主板、深圳主板、创业板。排除ST、*ST、其他风险警示、退市整理/已退市、科创板、北交所。结合代码、市场字段、证券状态判断。

## 第一层：硬性筛选
全部满足：
- 总市值50亿～200亿元（总市值，不是流通市值）
- 当日涨幅1%～6%
- 量比1.2～1.5
- 换手率5%～15%

## 第二层：成交量
近3交易日成交量持续放大，原则 V0 > V1 > V2。14:45模式不得用未收盘量机械比较昨日全天量，应使用同一时刻同比、成交速率或预计全天量等无未来数据的方法。

识别“温和放量”而非异常爆量。可用预计当日量/过去5日均量量化，初始参考1.05～1.50，必须配置化；2倍、3倍等异常爆量淘汰或明显降分。

## 第三层：均线和趋势
计算MA5、MA10、MA20，核心要求 MA5 > MA10 > MA20；优先三条均线自身同步向上。识别最近20～60日 Swing High/Low 和 Higher High/Higher Low，输出 trend_up 与趋势评分，不能仅凭单日创新高判断。

## 第四层：分时强度
获取1分钟或5分钟数据并计算VWAP/分时均价线。
Above_VWAP_Ratio = 股价位于VWAP上方有效分钟数/有效分钟数，默认参考≥70%，参数可配置。
额外计算13:00～14:45 Afternoon_Above_VWAP_Ratio，识别上午强、下午持续跌破VWAP、尾盘拉回以及冲高回落。

## 第五层：相对大盘强弱
上海主板→上证指数；深圳主板→深证成指；创业板→创业板指。
用分钟数据计算 Stock_Return(t)-Index_Return(t)。考虑上涨领先、指数回落时抗跌、反弹时强度、午后持续超额、14:45累计超额收益、相对最大回撤。输出 Relative_Strength_Intraday 和0～100 RS_Score。

## 第六层：支撑压力
基于最近20～60交易日识别前高/前低、Swing High/Low、横盘平台、跳空缺口、密集成交区域。输出 Support_Price、Resistance_Price、Resistance_Distance=(Resistance_Price-Current_Price)/Current_Price。
Min_Resistance_Distance配置化，默认可研究3%～5%。避免刚顶重大压力位。已突破前高时结合成交量和价格结构区分有效突破、假突破、冲高回落。

## 综合评分
硬条件先筛，剩余标的0～100综合评分，包括 Trend、Volume、VWAP、Relative Strength、Support/Resistance、Price Structure。权重全部配置化。分类：A优先观察、B次级观察、C淘汰并记录原因。

## 输出
至少包括：代码、名称、市场、当前价格、总市值、涨跌幅、量比、换手率、当日/前1日/前2日成交量、预计全天量、5日均量、温和放量评分、MA5/10/20及方向、趋势评分、VWAP、Above VWAP Ratio、Afternoon Above VWAP Ratio、对应指数、相对指数强度、RS Score、最近支撑、最近压力、距离压力位、综合评分、分类、风险提示。

## 淘汰日志
保存每层淘汰股票和具体原因，例如量比超限、MA排列失败、距离前高过近。每天输出筛选漏斗：股票池总数→基础条件→成交量→趋势→分时→相对大盘→压力位→最终候选。

## 数据与数据库
优先SQLite，增量保存每日行情、历史K线、分钟K线、筛选结果、指标、淘汰原因、评分。优先研究AKShare、东方财富公开行情接口等合法可用来源，设计主/备用源、重试、超时、异常处理、字段校验、缺失检查。单股失败不能导致全程序退出。必须核对总市值、量比、换手率、分钟K、复权方式定义和单位，严禁虚构缺失数据。

## 配置
建立config.yaml，至少包含市值、涨幅、量比、换手率、AboveVWAP、VolumeExpansion、MinResistanceDistance、MA周期、趋势周期、Swing参数、评分权重等，核心参数不得硬编码。

## 回测
支持 `python main.py --date 2026-08-25` 和 `python main.py --backtest 2026-01-01 2026-08-25`。严格避免Look-Ahead Bias，历史14:45只能使用当时已产生的数据。预留T+1/T+2/T+3/T+5/T+10收益、MFE、MAE统计，但当前不得根据未来收益自动改条件。

## 项目结构
模块化设计：main.py、config.yaml、src/data_fetcher.py、universe_filter.py、volume_filter.py、trend_filter.py、intraday_filter.py、relative_strength.py、support_resistance.py、scoring.py、database.py、report.py、tests/、data/、database/等，可合理优化。

最终候选生成技术图：日K+MA5/10/20+成交量+支撑压力；当日个股分时+VWAP+对应指数标准化分时。

## Windows使用
提供README.md以及run_1445.bat、run_close.bat等一键脚本。README面向非程序员说明Python/依赖安装、初始化、14:45/盘后运行、参数修改、结果查看、数据源切换。

## 开发顺序
1. 数据源连接 + 全A股票池 + 基础行情
2. 第一层硬筛并用真实行情验证
3. 历史日K + 成交量 + MA + 趋势
4. 分钟K + VWAP + 分时强度
5. 指数分钟数据 + 相对强弱
6. 支撑/压力算法
7. 综合评分 + 淘汰日志 + CSV/Excel报告
8. SQLite
9. 历史回测
10. Windows一键运行/定时执行

每阶段先用真实数据测试字段和计算结果，再进入下一阶段。不得为了“能运行”使用假数据。

## 第一项验收任务
基础数据功能完成后首先复盘2026-08-25，按完整条件输出收盘后候选，并输出 `2026-08-25_filter_log` 展示各层淘汰数量和原因。若最终0只，明确输出“2026-08-25：无完全符合标的。”并列出最接近全部条件的前10只及失败条件。

## 核心原则
可靠数据 → 可解释筛选 → 每日记录 → 长期统计 → 后续优化。任何入选/淘汰必须可追溯。禁止自动下单、连接券商交易账户和自动买卖。
